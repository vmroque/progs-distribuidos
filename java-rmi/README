Aplicação Java RMI

Aplicação na qual um cliente manipula 
a cotação de moedas apresentadas em
um servidor.

A aplicação usa a biblioteca Java
RMI (Remote Method Invocation) padrão
da JDK.

Passos para executar:

1 - Compilar o código
	$- javac *.java

2 - Executar a aplicação servidora
	$- java -Djava.rmi.server.hostname=<ip> ServerGUI

3 - Executar a aplicação cliente
	$- java ClientGUI
